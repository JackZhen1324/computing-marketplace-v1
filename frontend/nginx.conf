server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Disable sendfile for better compatibility with reverse proxies
    sendfile off;
    tcp_nopush on;
    tcp_nodelay on;

    # Gzip compression - disable for debugging reverse proxy issues
    gzip off;
    gzip_vary off;
    # gzip_min_length 1024;
    # gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    # Add header to disable keepalive for better compatibility
    add_header Connection "close" always;

    # API proxy - forward /api requests to backend service
    location /api/ {
        # Proxy to backend service in Docker network
        proxy_pass http://backend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection '';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Buffering settings to prevent incomplete chunked encoding
        proxy_buffering off;
        proxy_request_buffering off;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # Error handling
        error_page 502 503 504 = /api_error;
    }

    # API error endpoint
    location = /api_error {
        return 503 '{"success":false,"message":"后端服务暂时不可用，请稍后重试。如果问题持续，请联系管理员。"}';
        add_header Content-Type application/json;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        # Disable buffering for static assets to prevent incomplete transfers
        add_header Connection "close" always;
        # Increase timeout for large files
        sendfile_max_chunk 1m;
    }

    # SPA fallback - all routes go to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Disable access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}
